// TODO: Need to validate these return types at a later date.
class jQuery {

  length(): jQuery {
  }

  toArray(): jQuery {
  }

  get(num): jQuery {
  }

  pushStack(elems): jQuery {
  }

  each(callback): jQuery {
  }

  map(callback): jQuery {
  }

  slice(): jQuery {
  }

  first(): jQuery {
  }

  last(): jQuery {
  }

  even(): jQuery {
  }

  odd(): jQuery {
  }

  eq(i): jQuery {
  }

  end(): jQuery {
  }

  push(): jQuery {
  }

  sort(): jQuery {
  }

  splice(): jQuery {
  }

  extend(): jQuery {
  }

  find(selector): jQuery {
  }

  filter(selector): jQuery {
  }

  not(selector): jQuery {
  }

  is(selector): jQuery {
  }

  init(selector, context, root): jQuery {
  }

  has(target): jQuery {
  }

  closest(selectors, context): jQuery {
  }

  index(elem): jQuery {
  }

  /**
   *
   * @param {string} selector
   * @param context
   */
  add(selector, context): jQuery {
  }

  addBack(selector): jQuery {
  }

  parent(until, selector): jQuery {
  }

  parents(until, selector): jQuery {
  }

  parentsUntil(until, selector): jQuery {
  }

  next(until, selector): jQuery {
  }

  prev(until, selector): jQuery {
  }

  nextAll(until, selector): jQuery {
  }

  prevAll(until, selector): jQuery {
  }

  nextUntil(until, selector): jQuery {
  }

  prevUntil(until, selector): jQuery {
  }

  siblings(until, selector): jQuery {
  }

  children(until, selector): jQuery {
  }

  contents(until, selector): jQuery {
  }

  ready(fn): jQuery {
  }

  data(key, value): jQuery {
  }

  removeData(key): jQuery {
  }

  queue(type, data): jQuery {
  }

  dequeue(type): jQuery {
  }

  clearQueue(type): jQuery {
  }

  promise(type, obj): jQuery {
  }

  show(speed, easing, callback): jQuery {
  }

  hide(speed, easing, callback): jQuery {
  }

  toggle(speed, easing, callback): jQuery {
  }

  on(types, selector, data, fn): jQuery {
  }

  one(types, selector, data, fn): jQuery {
  }

  off(types, selector, fn): jQuery {
  }

  detach(selector): jQuery {
  }

  remove(selector): jQuery {
  }

  text(value): jQuery {
  }

  append(): jQuery {
  }

  prepend(): jQuery {
  }

  before(): jQuery {
  }

  after(): jQuery {
  }

  empty(): jQuery {
  }

  clone(dataAndEvents, deepDataAndEvents): jQuery {
  }

  html(value): jQuery {
  }

  replaceWith(): jQuery {
  }

  appendTo(selector): jQuery {
  }

  prependTo(selector): jQuery {
  }

  insertBefore(selector): jQuery {
  }

  insertAfter(selector): jQuery {
  }

  replaceAll(selector): jQuery {
  }

  css(name, value): jQuery {
  }

  fadeTo(speed, to, easing, callback): jQuery {
  }

  animate(prop, speed, easing, callback): jQuery {
  }

  stop(type, clearQueue, gotoEnd): jQuery {
  }

  finish(type): jQuery {
  }

  slideDown(speed, easing, callback): jQuery {
  }

  slideUp(speed, easing, callback): jQuery {
  }

  slideToggle(speed, easing, callback): jQuery {
  }

  fadeIn(speed, easing, callback): jQuery {
  }

  fadeOut(speed, easing, callback): jQuery {
  }

  fadeToggle(speed, easing, callback): jQuery {
  }

  delay(time, type): jQuery {
  }

  attr(name, value): jQuery {
  }

  removeAttr(name): jQuery {
  }

  prop(name, value): jQuery {
  }

  removeProp(name): jQuery {
  }

  addClass(value): jQuery {
  }

  removeClass(value): jQuery {
  }

  toggleClass(value, stateVal): jQuery {
  }

  hasClass(selector): jQuery {
  }

  val(value): jQuery {
  }

  trigger(type, data): jQuery {
  }

  triggerHandler(type, data): jQuery {
  }

  serialize(): jQuery {
  }

  serializeArray(): jQuery {
  }

  wrapAll(html): jQuery {
  }

  wrapInner(html): jQuery {
  }

  wrap(html): jQuery {
  }

  unwrap(selector): jQuery {
  }

  load(url, params, callback): jQuery {
  }

  offset(options): jQuery {
  }

  position(): jQuery {
  }

  offsetParent(): jQuery {
  }

  scrollLeft(val): jQuery {
  }

  scrollTop(val): jQuery {
  }

  innerHeight(margin, value): jQuery {
  }

  height(margin, value): jQuery {
  }

  outerHeight(margin, value): jQuery {
  }

  innerWidth(margin, value): jQuery {
  }

  width(margin, value): jQuery {
  }

  outerWidth(margin, value): jQuery {
  }

  ajaxStart(fn): jQuery {
  }

  ajaxStop(fn): jQuery {
  }

  ajaxComplete(fn): jQuery {
  }

  ajaxError(fn): jQuery {
  }

  ajaxSuccess(fn): jQuery {
  }

  ajaxSend(fn): jQuery {
  }

  bind(types, data, fn): jQuery {
  }

  unbind(types, fn): jQuery {
  }

  delegate(selector, types, data, fn): jQuery {
  }

  undelegate(selector, types, fn): jQuery {
  }

  hover(fnOver, fnOut): jQuery {
  }

  blur(data, fn): jQuery {
  }

  focus(data, fn): jQuery {
  }

  focusin(data, fn): jQuery {
  }

  focusout(data, fn): jQuery {
  }

  resize(data, fn): jQuery {
  }

  scroll(data, fn): jQuery {
  }

  click(data, fn): jQuery {
  }

  dblclick(data, fn): jQuery {
  }

  mousedown(data, fn): jQuery {
  }

  mouseup(data, fn): jQuery {
  }

  mousemove(data, fn): jQuery {
  }

  mouseover(data, fn): jQuery {
  }

  mouseout(data, fn): jQuery {
  }

  mouseenter(data, fn): jQuery {
  }

  mouseleave(data, fn): jQuery {
  }

  change(data, fn): jQuery {
  }

  select(data, fn): jQuery {
  }

  submit(data, fn): jQuery {
  }

  keydown(data, fn): jQuery {
  }

  keypress(data, fn): jQuery {
  }

  keyup(data, fn): jQuery {
  }

  contextmenu(data, fn): jQuery {
  }

}

export default jQuery;
